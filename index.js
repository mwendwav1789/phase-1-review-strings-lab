const currentUser = "Grace Hopper";
const welcomeMessage = `Welcome to Flatbook, ${currentUser}!`; // Use backticks for interpolation
const excitedWelcomeMessage = `WELCOME TO FLATBOOK, ${currentUser.toUpperCase()}!`; // Uppercase the user name
const shortGreeting = `Welcome, ${currentUser.slice(0, 1)}!`; // Get the first initial
